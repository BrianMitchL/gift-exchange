{
  "name": "gift-exchange",
  "version": "0.1.0",
  "description": "A command-line utility to generate pairs of names for a gift exchange.",
  "keywords": ["gift", "exchange", "secret", "santa"],
  "repository": {
    "type" : "git",
    "url" : "https://github.com/BrianMitchL/gift-exchange.git"
  },
  "main": "index.js",
  "scripts": {
    "prettier": "prettier --write *.{js,json,md}"
  },
  "author": "Brian Mitchell <bman4789@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "knuth-shuffle": "^1.0.8",
    "load-json-file": "^6.0.0"
  },
  "devDependencies": {
    "husky": "^2.3.0",
    "lint-staged": "^8.1.7",
    "prettier": "^1.17.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
